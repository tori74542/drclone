import { defineConfig } from 'vite';

export default defineConfig({
    // Base path for GitHub Pages deployment
    // If your repo is at https://github.com/user/repo, this should be '/repo/'
    // Using './' allows it to work in any subdirectory, which is usually sufficient for this type of app.
    base: './',
});
